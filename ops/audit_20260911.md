# 仓库安全审计报告 · judickzhu/guhai
日期:2026-09-11 ｜ 审计方式:远端(工作区被 macOS TCC 锁住)

## 一、基本事实
- 仓库:**public**,体积 **206 MB**,GitHub Pages **已启用**
- 根目录站 = **股海雙書知識庫**(商业产品,DCOGAI/DC姐姐);**红楼梦(一起讀紅樓白話)只是子目录 `/honglou/`**
- GitHub Pages 会伺服仓库内**任何**文件(.md/.txt/.json 均实测 HTTP 200 可下载)

## 二、🔴 严重发现

### 1. 产品内核公开可下载(实测 200)
| 文件 | 说明 |
|---|---|
| `v31_prompt.txt`(57KB) | **DC姐姐系统提示词全文** |
| `DC姐姐V4.0第二階段_20種人格模型主檔.md`(33KB) | 20 种人格模型主档 |
| `DC姐姐V4.0_*` (数十个 .md) | 人格碰撞/盲测/假问题识别/状态跳转训练与报告 |
| `DCOGAI開發需求單.md`/`轉達*.md`/`轉達.txt`/`郵件正文.txt` | 产品需求与内部沟通 |
| `DC姐姐X帳號內容包v1~v3.md` | 营销内容包 |
| `v3*_*.js` / `v4*_*.js` / `*_results.jsonl` / `*_judged.json` | 内部测试集、红队测试与失败案例(数百个) |
| `backend_snapshot_*.json` ×2(各 1.8MB) | 后端快照 |
| `qa_log.json` / `kb_queries.jsonl` / `kb_selfreview_*` | 使用日志与内部评审 |
| `kb-data_simplified.json`(3.3MB)/ `dc-kb.html`(1.3MB) | 知识库本体 |

### 2. 主动邀请搜索引擎抓取
```
robots.txt → User-agent: * / Allow: /
sitemap.xml → 518 个 URL + Sitemap 声明
```
→ 上述内容会被 Google 收录,而非「藏在冷门 URL」。

### 3. 硬编码凭证(公开仓库)
`tools/gen_yuanwen_from_ima.py:22`
```
KB = "ntEST…(32 chars)…E="     ← 明文
```
同文件 109-110 行却从 `~/.config/ima/client_id`、`api_key` 读取 → 该 hardcode 是遗留。
**git 历史永久保留,删文件不等于清除。**

## 三、🟠 中度
| 项 | 问题 |
|---|---|
| D | 红樓站与商业站同域共仓:读者上跳一级即见商业资料 |
| E | **生成器 `build_honglou_site.py` 未版本化**(单点故障,本轮事故已验证) |
| F | `.DS_Store` 入库 |
| G | 大文件:60MB PDF(honglou/)、3.4/3.3/3.1/3.0/1.8/1.8/1.5/1.3 MB 各若干 |
| H | `admin_server.py` 公开(无凭证,但暴露后台流程) |

## 四、⚠️ 轻度
- 子嗥「補充」会 POST 到 `http://127.0.0.1:8701`,访客本机若有服务会被写入(低危)
- 前端 JS(auth.js/dc-chat.js/app.js 等)**未发现**嵌入 API key ✓
- 红樓站自身**未发现**凭证 ✓;`honglou/qa_feedback` 为空 ✓

## 五、处置计划

### Phase 1 — 现在就能做(纯 GitHub 网页,不需工作区)
1. **轮换 ima KB key**(最优先)
2. **网页删除**(逐个 Delete file → Commit):`v31_prompt.txt`、`DCOGAI*`、`DC姐姐*`、`v3*_*`/`v4*_*`、`*_results.jsonl`、`*_judged.json`、`backend_snapshot*`、`qa_log.json`、`kb_queries.jsonl`、`kb_selfreview*`、`chain81_*`、`verify_*.js`、`rebuild_backend*.js`、`website_tmp_fix*.js`、`site_full_rebuild*.js`
3. **`robots.txt` 改 `Disallow: /`**;删 `sitemap.xml`(或仅留红樓)
4. **评估转 private**(会同时下线红樓站,可止血)
5. 若已收录:Search Console 提交移除

### Phase 2 — 工作区恢复后(我执行)
1. **拆仓**:`honglou/` + 生成器 + 素材 → 独立仓库(如 `guhai-honglou`),彻底分域
2. **生成器入库** `tools/build_honglou_site.py`(消除单点故障)
3. **清历史** `git filter-repo --path v31_prompt.txt --path-glob 'v3*_*' …`(需强推,协作者重克隆)
4. **大文件迁 Releases**(60MB PDF)
5. `.gitignore` 补:`.DS_Store`、`*_results.jsonl`、`*_judged.json`、`backend_snapshot*`、`qa_log.json`、`site_full_rebuild*`、`*.pdf`

## 六、诚实边界
- 上述文件若已被 clone/索引,**应假设已泄露**;对策是**轮换凭证 + 拆仓**,而非仅删文件
- 本地侧审计(密钥文件、.env、临时文件、后台日志)待工作区恢复后进行
