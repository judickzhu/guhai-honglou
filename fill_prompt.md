# 《紅樓夢》每回原文填寫 Prompt（防死循環版）

> 用途：貼到 dsh web 新開一個會話，讓 agent 把每一回「原文（無標點、無分段）」填入 `honglou_yuanwen.json`。
> 設計原則：路徑寫死、每回一步、失敗即停、禁重複讀、禁憑記憶編造。

---

## Prompt 正文（直接複製以下引號內容）

```
把《紅樓夢》每一回的「原文（無標點、無分段）」填入 honglou_yuanwen.json。嚴格按下列步驟執行，禁止任何變通：

1. 來源檔案路徑我已經準備好：每一回一個 txt，放在 `網站/honglou/yuanwen_sources/` 目錄下，檔名為 `第1回.txt`、`第2回.txt` … `第120回.txt`（或 `001.txt`…`120.txt`）。只讀這個目錄，不要自己猜測或拼接其他路徑。

2. 對「yuanwen_sources 裡存在的每一個回目檔」：
   a. 用 Read 工具讀取該 txt 的完整內容（每個檔只讀一次）。
   b. 抽取純漢字：去掉所有標點符號、空白、換行，只保留漢字與阿拉伯數字，得到一段連續無標點無分段的字串。
   c. 把這段字串寫入 `honglou_yuanwen.json` 中對應的數字鍵（"1" 到 "120"）。
   d. 寫完立即打印一行：`第N回 已寫入 X字`。

3. 來源目錄裡「不存在」的回目：不要處理、不要等待、不要補任何內容，直接跳過。

4. 所有存在的回目處理完畢後，打印 `全部完成：共處理 Y 回`，然後停止。

5. 若某個回目檔讀取失敗或內容為空：立即打印 `第N回 來源缺失，中止` 並停止任務。禁止重試、禁止回頭重讀同一檔、禁止進入任何等待或循環。

絕對禁止事項：
- 禁止在思考（think）裡反覆說「我要讀文件」「讀一下 honglou_yuanwen.json」卻不實際呼叫 Read 工具。
- 禁止對同一個檔案重複讀取超過一次。
- 禁止在沒真正讀到來源內容時，憑記憶或推測「編造」原文填入 json。
- 禁止去改動 `honglou_yuanwen.json` 裡「來源目錄沒有對應檔」的回目（那些回保持現狀）。
```

---

## 配套（更穩的做法：直接用腳本，完全繞開 agent）

若不想依賴 agent，直接在本機跑腳本批量生成，零死循環風險：

```bash
cd "/Users/macbookair/Downloads/电子书ipa/网站"
python3 tools/gen_yuanwen_json.py            # 從 yuanwen_sources/ 補
python3 tools/gen_yuanwen_json.py --from-chapters   # 另從 chapters/NNN.html 補「空」回（楔子級兜底）
```

腳本特性：讀檔失敗立即退出不重試、無來源回跳過、冪等、寫前自動備份 `honglou_yuanwen.json.bak`。

---

## 線上死循環會話的處置

那個卡在「讀文件」空轉的會話是 dsh web 線上端會話（未落盤，故本地會話檔搜不到）。在 dsh web 界面（http://127.0.0.1:3080）左側會話列表裡找到「紅樓夢需要設計一個每一回的」那條，點停止 / 刪除，重新開一個會話貼上面的 prompt 即可。不要用原會話繼續跑。
